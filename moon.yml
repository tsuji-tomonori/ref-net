workspace:
  projects:
    - package/*

tasks:
  install:
    command: |
      cd package/shared && uv sync --dev
      cd ../api && uv sync --dev
      cd ../crawler && uv sync --dev
      cd ../summarizer && uv sync --dev
      cd ../generator && uv sync --dev
    options:
      cache: false
